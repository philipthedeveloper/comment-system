import React, { useContext } from "react";
import { Database } from "../constants/Context";

export default function Comments() {
  // const { comments } = useContext(Database);
  return (
    <div>
      <h3>Comments</h3>
    </div>
  );
}
